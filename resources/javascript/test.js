// alert('Uwu. Go follow our insta for updates: @Dojoanime');

const change = () => {
    let randomColor = Math.floor(Math.random()*16777215).toString(16);
    document.body.style.backgroundColor = '#' + randomColor;
  }
  // changeBg.addEventListener("click", change);
  // change();
  